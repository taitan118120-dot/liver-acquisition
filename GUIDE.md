# TAITAN PRO 集客自動化 運用ガイド

最終更新: 2026-08-12（2026-04-04版は現行方針と矛盾するため全面書き換え）

## この文書の役割

X(Twitter)とInstagramの自動化・手動運用の「今の姿」をまとめたもの。個別スクリプトの実装詳細は
各ファイルのコメントを参照。

## やらないこと（禁止事項）

- **自動いいね** — 非効率と判断し廃止済み（`cloud_engage.py` はいいね機能を削除済み）
- **自動リプライ送信** — X利用規約上BAN対象。候補の選定は自動化するが、送信は必ず人間が行う
- **自動フォロー** — 2026-04-15停止。フォローバック率がほぼ0で効果が出なかった
  （`auto_follow.yml` は名前も「自動フォロー (停止中)」に変更し、schedule をコメントアウトして手動dispatchのみ残している）
- **コールドDM（新規開拓のDM営業）** — 2026-06-08にPush型からPull型へ転換して以降やっていない。
  現在の流入は「相手から応募してもらう」導線が前提

## X(Twitter) — 現在稼働中の自動化

| 内容 | スクリプト / ワークフロー | 頻度 | 人間の作業 |
|---|---|---|---|
| 投稿 | `cloud_post.py` / `auto_post.yml` | 毎日3回（JST 8/13/20時、着弾時刻にジッターあり） | なし（完全自動） |
| リード検索（いいねなし） | `cloud_engage.py` / `auto_engage.yml` | 毎日2回 | CSVに溜まったリードは見るだけ。自動アクションはしない |
| 公開リストへの追加 | `cloud_list_add.py` / `auto_list_add.yml` | 毎日 | なし（リスト追加は相手に通知が届き、プロフィールを見に来てもらう狙いの施策） |
| フォロバなしの整理 | `cloud_unfollow_noback.py` / `auto_unfollow.yml` | 毎日 | なし（フォロー/フォロワー比を健全に保つための後処理で、新規フォロー施策ではない） |
| リプ候補の抽出 | `x_reply_digest.py` / `x_reply_digest.yml` | 毎日 JST 7:00 | **ここが本体。** GitHub Issueで届く候補から実際にリプする投稿を選び、文面も手動で書いて送信する |

### 手動リプ運用（2026-08-08〜、現在のメイン施策）

X上でフォロワーを増やす手段は実質リプライ・引用のみで、自動送信は規約違反。また「投稿内容を改善しても
流入は増えない」ことが2026-08-08の健康診断（フォロワー17・リーチ率147%＝抑制はされていない）で実測済み。
そのため**「誰にリプすべきか選ぶ」までを自動化し、送信だけは人力**にしている。

1. 毎朝GitHub Issue（ラベル `x-reply-digest`）に候補が届く
2. 共感リプ（フォロワー10〜5000の、始めたばかり・悩んでいる本人）と
   露出リプ（フォロワー500+の、反応が付き始めている投稿）が混在して出てくる
3. 1日10分程度、自分の言葉でリプを送る（テンプレ流し込みはしない）
4. 効果測定は `x_health_check.yml` を手動実行してフォロワー数・リーチ率を確認する

候補0件の日はIssueが作られない仕様（空Issueが続くと開かれなくなるため）。

## Instagram — 現在稼働中の自動化

| 内容 | スクリプト / ワークフロー | 頻度 |
|---|---|---|
| バズ特化カルーセル投稿 | `instagram/ig_viral_generator.py` + `ig_poster.py` / `instagram_post.yml` | 週3回（月・水・金 20:00 JST） |

2026-05-11にIGから警告（サイバーセキュリティ規定違反の分類）を受けた経緯があるため、以下を厳守する：

- 投稿頻度は**週3回が上限**（警告履歴がある間はここで止める。増やすとしても段階的に）
- キャプションでDM誘導・LINE誘導・「無料相談」の連呼をしない
- 「最短」「即日」「絶対」「保証」等の断定・即効性表現を使わない
- ハッシュタグは毎回同じセットを使い回さない。「#副業」系タグは1投稿につき最大1個

コンテンツは2026-07-11に「単画像のNote記事要約＋無料相談CTA」から「バズ特化カルーセル」へ全面刷新済み
（旧方式は数ヶ月運用して獲得0件だったため廃止）。数字・実績は事実確認済みのもの（還元率100%、承認済み実例等）
だけを使う。

## 効果測定

コールドDM前提の固定KPI数値目標（フォロワー◯人・DM送信◯通/月）は現行方針と矛盾するため撤去した。
現状は以下のように判断している：

- **X**: `x_health_check.yml` を手動実行してフォロワー数・リーチ率を確認。手動リプ運用開始(2026-08-08)
  から2週間程度を目安に効果測定し、伸びていなければX自体への投資配分を見直す
- **Instagram**: 投稿ごとの保存数・シェア数を見て、伸びたテーマ（あるある/ぶっちゃけ/診断 等）に寄せる
- **入会経路全体**: AI検索(GEO)経由・紹介・広告などチャネル別の実績は都度 `data/` 配下のレポートで確認する
