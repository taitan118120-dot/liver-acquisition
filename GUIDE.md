# TAITAN PRO 集客自動化 運用ガイド

最終更新: 2026-09-16（402を飲み込んでいたリプ候補・リスト追加を停止扱いに合流）

## この文書の役割

X(Twitter)とInstagramの自動化・手動運用の「今の姿」をまとめたもの。個別スクリプトの実装詳細は
各ファイルのコメントを参照。

## やらないこと（禁止事項）

- **自動いいね** — 非効率と判断し廃止済み（`cloud_engage.py` はいいね機能を削除済み）
- **自動リプライ送信** — X利用規約上BAN対象。候補の選定は自動化するが、送信は必ず人間が行う
- **自動フォロー** — 2026-04-15停止。フォローバック率がほぼ0で効果が出なかった
  （`auto_follow.yml` は名前も「自動フォロー (停止中)」に変更し、schedule をコメントアウトして手動dispatchのみ残している）
- **コールドDM（新規開拓のDM営業）** — 2026-06-08にPush型からPull型へ転換して以降やっていない。
  現在の流入は「相手から応募してもらう」導線が前提

## X(Twitter) — ⏸ 2026-08-23からAPIクレジット枯渇で実質停止中

> **X API が読み書きとも `402 Payment Required / credits depleted` を返す状態が
> 2026-08-23から続いている。** 2026-09-15にユーザー判断で「課金しない」を選択したため、
> 下の表は**クレジットが戻ったときの姿**であって、いま動いているものではない。
> 課金を回復すれば cron は止めていないので**次のランから全部が自動で生き返る**。
>
> 再開条件・番犬との関係は下の「X API 402（クレジット枯渇）と番犬の関係」。

| 内容 | スクリプト / ワークフロー | 頻度 | 人間の作業 | 402中の実態 |
|---|---|---|---|---|
| 投稿 | `cloud_post.py` / `auto_post.yml` | 毎日3回（JST 8/13/20時、着弾時刻にジッターあり） | なし（完全自動） | 投稿0本。exit 75 で赤にせずIssue 1本に集約 |
| リード検索（いいねなし） | `cloud_engage.py` / `auto_engage.yml` | 毎日2回 | CSVに溜まったリードは見るだけ。自動アクションはしない | 検索が全件402。リードは増えていない |
| 公開リストへの追加 | `cloud_list_add.py` / `auto_list_add.yml` | 毎日 | なし（リスト追加は相手に通知が届き、プロフィールを見に来てもらう狙いの施策） | 追加0件。exit 75 で赤にせずIssue #51 に合流し、サマリに「停止中」と出る |
| リプ候補の抽出 | `x_reply_digest.py` / `x_reply_digest.yml` | 毎日 JST 7:00 | **ここが本体。** GitHub Issueで届く候補から実際にリプする投稿を選び、文面も手動で書いて送信する | 候補0件。exit 75 で赤にせずIssue #51 に合流し、サマリに「停止中」と出る |

> ⚠️ **CIの色でX施策の生死を判断しない。** 402中のランは意図的に緑にしている
> （赤にする打ち手は Issue #45 がコメント81件になった件で失敗済み）。
> 生きているかどうかは **Issue #51 が開いているか**と、**各ランの「結果サマリ」**で見る。
>
> 2026-09-16の修正前は `auto_list_add` と `x_reply_digest` が402を「検索結果0件」として
> 飲み込んでいたため、CIが緑のまま**リプ候補Issueが3週間1本も出ない**状態に誰も気づけなかった
> （実測: run 34912969025 は全8クエリが402、run 34915986059 は全31キーワードが402、どちらも success）。
> 現在はこの2本も402を `exit 75` で区別し、「検索が全件402で0件」と
> 「検索は通ったが候補0件」がログとサマリで必ず読み分けられる。

> 廃止（2026-09-06）: 「フォロバなしアンフォロー」（`auto_unfollow.yml`）は削除した。
> X APIのクレジット枯渇（402 credits depleted）で2026-08-23以降ずっと失敗し、
> 毎日の失敗通知メールを増やすだけになっていたため。
> スクリプト `cloud_unfollow_noback.py` は手動実行用に残してある。

### 手動リプ運用（2026-08-08〜、現在のメイン施策）

X上でフォロワーを増やす手段は実質リプライ・引用のみで、自動送信は規約違反。また「投稿内容を改善しても
流入は増えない」ことが2026-08-08の健康診断（フォロワー17・リーチ率147%＝抑制はされていない）で実測済み。
そのため**「誰にリプすべきか選ぶ」までを自動化し、送信だけは人力**にしている。

1. 毎朝GitHub Issue（ラベル `x-reply-digest`）に候補が届く
2. 共感リプ（フォロワー10〜5000の、始めたばかり・悩んでいる本人）と
   露出リプ（フォロワー500+の、反応が付き始めている投稿）が混在して出てくる
3. 1日10分程度、自分の言葉でリプを送る（テンプレ流し込みはしない）
4. 効果測定は `x_health_check.yml` を手動実行してフォロワー数・リーチ率を確認する

候補0件の日はIssueが作られない仕様（空Issueが続くと開かれなくなるため）。
**Issueが来ない日は、ランの「結果サマリ」で理由を確認すること。** 0件には意味の正反対な2種類がある。

| サマリの表示 | 終了コード | 意味 |
|---|---|---|
| 🔍 検索は通りましたが、条件を満たす候補が0件 | 2 | APIは生きている。今日は良い候補が無かっただけ |
| ⏸ 動いていません — クレジット枯渇(402)で検索が全滅 | 75 | APIが死んでいて母数がゼロ。Issue #51 に合流済み |

（2026-09-15まではこの2つが区別されておらず、402の3週間が「候補0件の日が続いている」と
同じ見え方をしていた。）

### X API 402（クレジット枯渇）と番犬の関係

402は**課金しないと絶対に直らず、課金したら次のランで勝手に直る**種類の詰まりで、
コード側でできることが1つも無い。2026-08-23からの3週間で、これを「失敗」として扱った
ものが順に潰れていった（`auto_post.yml` はIssue #45 がコメント81件、
`auto_unfollow.yml` は廃止、プロフィール番犬は毎日赤）。

そこで402だけは**赤にせず「停止中」として別枠で可視化する**形に統一している。

| 場所 | 402のときどうなるか |
|---|---|
| `auto_post.yml` | exit 75（`x_credits.EXIT_CREDITS_DEPLETED`）。赤にせずIssue 1本に集約し、投稿成功で自動クローズ |
| `x_reply_digest.yml` | exit 75。赤にせずIssue #51 に合流。サマリに「⏸ 動いていません」と出す |
| `auto_list_add.yml` | exit 75。同上 |
| `social_profile_guard.py` | X媒体を `suspended` 枠へ。`--require-live` でも赤にしないが、毎ラン「停止中の媒体」として表示しIssue本文にも載せる |

402の判定と終了コード75は **`x_credits.py` が正本**（`is_credits_depleted()` / `EXIT_CREDITS_DEPLETED`）。
集約Issueのタイトルと本文は **`.github/scripts/x_credits_issue.js` が正本**で、
3本のワークフローがこれを `require` する。タイトルをワークフローにコピペしないこと
（1文字でもずれると集約先を見失い、2本目のIssueが立って「通知は1本」の設計が壊れる）。

同じIssueを3本で共有するので、再検知コメントの重複判定は `guard_comment.js` の
`key`（発信元名）ごとに行う。key が無いと発信元が交互にコメントするたびに
「前回と違う」と判定され、黙らせたかったコメントが毎日積み増す。

**プロフィール番犬のスイッチは `config.X_API_CREDITS_DEPLETED_SUSPENDED`。**
これは**手で立てる停止スイッチではない**。
「X APIが実際に402を返した回だけ」降格する条件付きの仕掛けなので、

- **課金を回復したら、フラグを触らなくても次のランから通常検査に戻る**（戻し忘れの死角が無い）
- 401（トークン失効）・Secrets切れ・別アカウント参照は**今までどおり赤**。降格するのは402だけ
- このフラグを空文字に戻すのは「402でも赤くしたい」と判断したときだけ

#### 再開条件（＝この停止扱いをやめてよいとき）

以下のどちらか。**どちらもコードの変更は要らない。**

1. **課金を回復する** — https://developer.x.com/en/portal/dashboard で残クレジットを戻す。
   投稿・リプ候補・リスト追加・番犬が同時に生き返り、Issue #51 も自動クローズされる。
   番犬が緑に戻ることの確認は `gh workflow run social_profile_guard.yml --ref main`。
2. **Xの運用そのものをやめる** — その場合は停止扱いではなく、IGと同じ畳み方をする
   （ワークフロー削除＋正本 `marketing/social_profiles.md` のX節と番犬の `sources` から
   外す）。「動かさないのに毎日叩く」状態を残さない。手順の実例は
   [`instagram/RETIRED.md`](instagram/RETIRED.md)。

**放置してよい期限は設けていないが、402が続く限りXからの流入はゼロのままである**ことは
上の表のとおり。停止扱いは「気づかないまま止まる」を防ぐためのもので、
「止まっていてよい」という意味ではない。

## Instagram — 🛑 2026-09-16に撤去（もう何も動いていない）

> **事務所公式 `@taitan_pro7` が 2026-09-11 に Instagram 上から消滅**（削除または停止。
> トークンもIDも正常で、APIでは復旧不能）。09-15 に自動投稿を停止し、
> **09-16 にユーザー判断で恒久停止**＝ワークフロー6本・投稿スクリプト一式・
> 番犬のIG走査をすべて削除した。
>
> - 経緯・削除したファイル・残したファイル・作り直すときの手順は
>   → [`instagram/RETIRED.md`](instagram/RETIRED.md)（IGの正本はこれ1本）
> - **停止スイッチは無い**。`config.OFFICE_INSTAGRAM_SUSPENDED` も番犬ごと消した
> - `instagram/ig_posts.json` と `images/` は**記録として**残っているが、
>   投稿する側がもう無い（在庫ではない）
> - 再開するなら復活ではなく作り直し。実測では
>   **フォーマットも投稿時刻も効かず、天井はフォロワー数**だった
>   （[[project_ig_insights_baseline]]）ので、やる前にそこを読むこと

## 効果測定

コールドDM前提の固定KPI数値目標（フォロワー◯人・DM送信◯通/月）は現行方針と矛盾するため撤去した。
現状は以下のように判断している：

- **X**: `x_health_check.yml` を手動実行してフォロワー数・リーチ率を確認。手動リプ運用開始(2026-08-08)
  から2週間程度を目安に効果測定し、伸びていなければX自体への投資配分を見直す
- **Instagram**: 2026-09-16に撤去済み（[`instagram/RETIRED.md`](instagram/RETIRED.md)）。測るものは無い
- **入会経路全体**: AI検索(GEO)経由・紹介・広告などチャネル別の実績は都度 `data/` 配下のレポートで確認する
